{
  "name": "ww",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js --watch src --exec babel-node -e js,graphql --ignore *.test.js",
    "local": "ENV=LOCAL PORT=3000 nodemon index.js --watch src --exec babel-node -e js,graphql --ignore *.test.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "swagger": "node swagger.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "nodemon": "^2.0.7",
    "swagger-ui-express": "^4.1.6"
  },
  "devDependencies": {
    "@babel/node": "^7.13.12",
    "swagger-autogen": "^2.7.3"
  }
}
